var eventBus = require('../../eventBus');

/**
 * Wire up event bus to auth controller
 */
module.exports = function(controller) {

  /**
   * Event: get-pending-marks
   * 
   */
  // eventBus.on('get-pending-marks', (e) => {
  //   controller.getPendingMarks(e.sort, e.offset, e.limit, e.handler);
  // });

};